# Используем официальный образ PHP
FROM php:8.2-cli

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем все файлы из текущей директории в контейнер
COPY . .

# Разрешаем доступ к порту 80
EXPOSE 80

# Запускаем встроенный PHP-сервер
CMD ["php", "-S", "0.0.0.0:80", "-t", "."]
